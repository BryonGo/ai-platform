/**
 * 画布路由保留给直达链接和内部业务跳转使用。
 *
 * 画布入口已经从侧栏撤下，但不能因此拦截 /canvas：用户可能从作品、故事或历史链接
 * 进入画布。入口可见性和路由可访问性是两个独立问题，生产开关只负责控制入口，不再
 * 把合法的直达地址重定向到首页。
 */
export default defineNuxtRouteMiddleware(() => undefined)
