// 密码强度基线：与后端同一口径（≥8 位且同时含字母与数字）。
//
// 后端在 2026-09-15 的 fix(security) 里加了这条校验（注册/游客升级/改密/重置密码都走它）。
// 独立注册页与登录弹窗共用这里，避免两处各写一份、口径漂移
// （以前弹窗注册只判非空，用户按弹窗填 6 位会被服务端拒）。
export const PASSWORD_RE = /^(?=.*[A-Za-z])(?=.*\d).{8,}$/
export const PASSWORD_HINT = '至少 8 位，且需同时包含字母与数字'
